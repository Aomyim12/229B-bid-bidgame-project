using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class publicfloatblowSpeed=1.0f;voidUpdate(){เปลี่ยนตำแหน่งของGameObjectในแนวแกนyเพื่อเป่าtransform.Translate(0,blowSpeedTime.deltaTime,0);} : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
